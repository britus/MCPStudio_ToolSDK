{
  "annotations": {
    "audience": [
      "user",
      "assistant"
    ],
    "lastModified": "2026-04-17T08:19:00.000Z",
    "priority": 0.9
  },
  "name": "shell_execution",
  "description": "Run a shell command with explicit parameters through the MCPStudio shell bridge.",
  "type": "skill",
  "contextTags": [
    "shell",
    "command",
    "process"
  ],
  "scripts": [
    "shellCall.js"
  ],
  "relatedTools": [
    "config/Tools/shell_call.json"
  ],
  "handlers": [],
  "whenToUse": [
    "When a task cannot be completed through a dedicated higher-level tool.",
    "When the user needs a controlled command execution with recorded stdout and stderr."
  ],
  "instructions": [
    "Use explicit parameters instead of embedding untrusted input into a shell string when possible.",
    "Prefer purpose-built build or compiler skills when they cover the task more safely."
  ]
}
