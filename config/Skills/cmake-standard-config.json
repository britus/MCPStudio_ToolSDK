{
  "allowedToolNames" : [
    "skill_execute"
  ],
  "description" : "Runs a standard CMake configure step in a selected project directory.",
  "modelPreferences" : {

  },
  "name" : "cmake-standard-config",
  "promptTemplate" : "Create and execute the following shell script with `skill_execute`, passing it as `content`. Use `{{project_directory}}` as the CMake project directory.\n\n```bash\n#!\/usr\/bin\/env bash\nset -euo pipefail\nPROJECT_DIR=\"{{project_directory}}\"\n[ -n \"$PROJECT_DIR\" ] && [ \"$PROJECT_DIR\" != \"{{project_directory}}\" ] || { echo \"Missing project_directory\" >&2; exit 1; }\ncd \"$PROJECT_DIR\"\ncmake -S . -B build\n```\n\nReturn the CMake configure output.",
  "resourceNames" : [

  ],
  "skillEnabled" : true,
  "systemInstruction" : "Run a standard CMake configure command in the selected project directory.",
  "toolScopeMode" : "restricted",
  "triggerPhrases" : [
    "standard cmake config",
    "cmake config projekt directory",
    "cmake configure"
  ],
  "visibilityScope" : "chat"
}