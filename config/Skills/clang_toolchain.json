{
  "annotations": {
    "audience": [
      "user",
      "assistant"
    ],
    "lastModified": "2026-04-17T08:19:00.000Z",
    "priority": 0.9
  },
  "name": "clang_toolchain",
  "description": "Check syntax, compile source files, or run make-based builds with Clang-oriented helpers.",
  "type": "skill",
  "contextTags": [
    "clang",
    "c",
    "cpp",
    "make",
    "build"
  ],
  "scripts": [
    "clangTools.js"
  ],
  "relatedTools": [
    "config/Tools/clang_check_syntax.json",
    "config/Tools/clang_compile.json",
    "config/Tools/clang_make.json"
  ],
  "handlers": [
    "clangCheckSyntax",
    "clangCompile",
    "clangMake"
  ],
  "whenToUse": [
    "When the user wants a fast syntax-only check of C or C++ code.",
    "When a source file should be compiled or a Makefile-driven build should run."
  ],
  "instructions": [
    "Use syntax checking first when the goal is validation rather than producing build artifacts.",
    "Switch to make-based execution when the project already defines its build steps in a Makefile."
  ]
}
