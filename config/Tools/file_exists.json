{
  "annotations": {
    "audience": [
      "user",
      "assistant"
    ],
    "lastModified": "2026-04-07T04:25:53.899Z",
    "priority": 0.9
  },
  "description": "Check whether a file or directory exists.",
  "execHandler": "file_exists",
  "execMethod": "toolEntry",
  "inputSchema": {
    "type": "object",
    "properties": {
      "path": {
        "name": "path",
        "description": "File or directory path to check.",
        "type": "string"
      }
    },
    "required": [
      "path"
    ]
  },
  "name": "file_exists",
  "outputSchema": {
    "type": "object",
    "properties": {
      "exists": {
        "name": "exists",
        "description": "True when the path exists.",
        "type": "boolean"
      },
      "path": {
        "name": "path",
        "description": "Validated path that was checked.",
        "type": "string"
      },
      "success": {
        "name": "success",
        "description": "Status flag about operation.",
        "type": "boolean"
      }
    },
    "required": [
      "exists",
      "path",
      "success"
    ]
  },
  "pluginName": "",
  "scriptName": "Scripts/tool_entry.js",
  "toolType": "ScriptTool"
}
