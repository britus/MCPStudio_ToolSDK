{
  "arguments" : [
    {
      "description" : "Optional exact local file paths or file:\/\/ URIs. Leave empty to discover documents from RAG.",
      "name" : "document_sources",
      "required" : false
    },
    {
      "description" : "User-provided subject-matter context, including terminology, source constraints, expected behavior, and explicit exclusions.",
      "name" : "domain_context",
      "required" : false
    },
    {
      "description" : "Project-relative TOML training configuration.",
      "name" : "training_config",
      "required" : true
    },
    {
      "description" : "Plain-language description of the knowledge or documents to retrieve from RAG and train.",
      "name" : "training_goal",
      "required" : false
    },
    {
      "description" : "Select an absolute UTF-8 .txt or .md objective file with the file button. Its content is loaded by the release 3 objective-loader tool.",
      "name" : "training_objective_file",
      "required" : true
    }
  ],
  "description" : "Train the configured model adapter from a simple objective: retrieve matching RAG documents, generate fresh JSONL data, run smoke, and continue only on PASS.",
  "name" : "finetune_training_cycle",
  "template" : "Complete training workflow input. Copy every line between `<workflow_input>` and `<\/workflow_input>` unchanged into `run_workflow.user_prompt`; do not shorten it to the workflow name or omit any field.\n\n<workflow_input>\nProject root: `${CHAT_PROJECT_DIR}`\nTraining configuration: `{{training_config}}`\nTraining objective file: {{training_objective_file}}\nInline training objective: {{training_goal}}\n\nUser-provided subject-matter context (optional runtime data):\n{{domain_context}}\n\nExplicit local document paths or file:\/\/ URIs (optional):\n{{document_sources}}\n\nLoad and validate the selected objective file before document discovery. The inline objective is only a fallback when no file is supplied by a direct non-picker invocation. Build a generic source plan with requiredSubjects and exact sources containing path, subjects and evidence. Keep all product and domain facts in runtime input and selected documents. Require deterministic hash deduplication, source and subject balancing, and independent train\/validation coverage. Run smoke first, then full training only on PASS. Python owns metrics, early stopping and best-checkpoint selection. Do not merge, verify, install or deploy.\n<\/workflow_input>"
}
