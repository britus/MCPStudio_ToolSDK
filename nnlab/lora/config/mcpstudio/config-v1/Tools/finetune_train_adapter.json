{
  "annotations": {
    "audience": ["user", "assistant"],
    "lastModified": "2026-08-17T00:00:00.000Z",
    "priority": 0.95
  },
  "description": "Run a full or smoke-test MLX LoRA training cycle for the local fine-tuning project using a repository TOML configuration.",
  "execHandler": "finetuneTrainAdapter",
  "execMethod": "toolEntry",
  "inputSchema": {
    "properties": {
      "projectRoot": {
        "description": "Absolute fine-tuning project root. Defaults to this repository.",
        "name": "projectRoot",
        "type": "string"
      },
      "configPath": {
        "description": "Project-relative training TOML, for example config/huawei.toml.",
        "name": "configPath",
        "type": "string"
      },
      "smokeTest": {
        "description": "Run one conservative smoke-training iteration instead of the full configured training.",
        "name": "smokeTest",
        "type": "boolean"
      }
    },
    "type": "object"
  },
  "name": "finetune_train_adapter",
  "outputSchema": {
    "properties": {
      "success": {"description": "Whether training completed successfully.", "name": "success", "type": "boolean"},
      "operation": {"description": "Executed training operation.", "name": "operation", "type": "string"},
      "error": {"description": "Failure details when unsuccessful.", "name": "error", "type": "string"}
    },
    "required": ["success"],
    "type": "object"
  },
  "pluginName": "",
  "scriptName": "${CHAT_PROJECT_DIR}/config/mcpstudio/config-v1/Scripts/finetune_tools.js",
  "toolType": "ScriptTool"
}
