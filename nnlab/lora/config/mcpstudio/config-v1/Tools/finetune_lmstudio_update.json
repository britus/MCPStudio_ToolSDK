{
  "annotations": {
    "audience": ["user", "assistant"],
    "lastModified": "2026-08-17T00:00:00.000Z",
    "priority": 0.95
  },
  "description": "Transactionally update the existing offline LM Studio model: build and validate a replacement, preserve defaults, atomically swap, and roll back on failure.",
  "execHandler": "finetuneLmStudioUpdate",
  "execMethod": "toolEntry",
  "inputSchema": {
    "properties": {
      "projectRoot": {"description": "Absolute fine-tuning project root.", "name": "projectRoot", "type": "string"},
      "deployConfig": {"description": "Project-relative LM Studio deployment TOML.", "name": "deployConfig", "type": "string"},
      "dryRun": {"description": "Validate update paths and permit existing targets without changing them.", "name": "dryRun", "type": "boolean"}
    },
    "type": "object"
  },
  "name": "finetune_lmstudio_update",
  "outputSchema": {
    "properties": {
      "success": {"description": "Whether update completed successfully.", "name": "success", "type": "boolean"},
      "operation": {"description": "Executed update operation.", "name": "operation", "type": "string"},
      "error": {"description": "Failure details when unsuccessful.", "name": "error", "type": "string"}
    },
    "required": ["success"],
    "type": "object"
  },
  "pluginName": "",
  "scriptName": "${CHAT_PROJECT_DIR}/config/mcpstudio/config-v1/Scripts/finetune_tools.js",
  "toolType": "ScriptTool"
}
